This folder is reserved for input datasets.

Currently, the project relies on synthetic data generated by the FEM solver.

Future versions may include:
- real thermal datasets
- CAD-based geometry inputs
